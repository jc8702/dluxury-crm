# DATABASE CONFIG (NEON POSTGRESQL)
DATABASE_URL="postgres://user:password@hostname/dbname?sslmode=require"

# SECURITY CONFIG (CRM PIN)
APP_PIN="1234"

# SALESFORCE MOCK TOGGLE (Set to 'true' to force failures for SRE testing)
# VITE_FORCE_SF_FAILURE=false
