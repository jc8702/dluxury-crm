import Dashboard from '../components/dashboard/Dashboard';

/**
 * DashboardPage — wrapper padronizado
 * Layout compartilhado (Layout.tsx) garante max-w 1440px e padding 16/24.
 * Header consistente via src/components/layout/Header.tsx
 */
const DashboardPage = () => <Dashboard />;
export default DashboardPage;
